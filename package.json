{
  "name": "monopoly",
  "version": "1.0.0",
  "description": "Monopoly Game - Backend and Frontend",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build:all": "cd backend && npm run build && cd ../frontend && npm run build"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
